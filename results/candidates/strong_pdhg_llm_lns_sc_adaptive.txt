policy_id: adaptive
comparison_role: llm4unroll_reference
origin: strong_baseline
split: train
score: -3.3546945152299457
aggregate: {'median_gap': 1.0, 'median_violation': 3.1622776601683795, 'median_runtime': 0.0018177032470703125, 'median_primal_residual': 3.1622776601683795, 'median_dual_residual': 0.0, 'median_iterations': 80.0, 'fail_rate': 0.0}
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    ratio = (state.get('r_p_norm', 0.0) + 1e-9) / (state.get('r_d_norm', 0.0) + 1e-9)
    if ratio > 1.5:
        actions.append({'name': 'scale_tau', 'value': 1.12})
        actions.append({'name': 'scale_sigma', 'value': 0.9})
    elif ratio < 0.7:
        actions.append({'name': 'scale_tau', 'value': 0.9})
        actions.append({'name': 'scale_sigma', 'value': 1.12})
    if state.get('stagnation_count', 0) >= 4:
        actions.append({'name': 'restart'})
        actions.append({'name': 'damp', 'value': 0.85})
    else:
        actions.append({'name': 'damp', 'value': 1.0})
    return {'actions': actions}

verified: True
errors: []
warnings: ['Action scale_tau is emitted without reading state feature tau.', 'Action scale_sigma is emitted without reading state feature sigma.', 'Action scale_tau is emitted without reading state feature tau.', 'Action scale_sigma is emitted without reading state feature sigma.', 'Required features not referenced by policy: k, sigma, tau']
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 0.9631581179169847, 'violation': 1.6464186103289626, 'runtime': 0.00015282630920410156}
feature_coverage: 0.4
proof_obligations: ["path[1]: fallback satisfied under if ratio > 1.5, if state.get('stagnation_count', 0) >= 4", "path[2]: fallback satisfied under if ratio > 1.5, else not (state.get('stagnation_count', 0) >= 4)", "path[3]: fallback satisfied under else not (ratio > 1.5), if ratio < 0.7, if state.get('stagnation_count', 0) >= 4", "path[4]: fallback satisfied under else not (ratio > 1.5), if ratio < 0.7, else not (state.get('stagnation_count', 0) >= 4)", "path[5]: fallback satisfied under else not (ratio > 1.5), else not (ratio < 0.7), if state.get('stagnation_count', 0) >= 4", "path[6]: fallback satisfied under else not (ratio > 1.5), else not (ratio < 0.7), else not (state.get('stagnation_count', 0) >= 4)"]
