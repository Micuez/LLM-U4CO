policy_id: llm_one_shot
origin: baseline
split: train
score: -1.0354005364952326
aggregate: {'median_gap': 0.0, 'median_violation': 0.0, 'median_runtime': 0.0015368461608886719, 'median_primal_residual': 0.0, 'median_dual_residual': 0.3104104032379415, 'median_iterations': 60.0, 'fail_rate': 0.0}
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    mip_gap = state.get('mip_gap', 0.0)
    fractionality = state.get('fractionality', 0.0)
    lp_gap = state.get('lp_gap', 0.0)
    if mip_gap > 0.18 and fractionality > 0.18:
        actions.append({'name': 'damp', 'value': 0.98})
    elif lp_gap < 0.08 and fractionality < 0.12:
        actions.append({'name': 'damp', 'value': 0.62})
    else:
        actions.append({'name': 'damp', 'value': 0.84})
    if state.get('stagnation_count', 0) >= 3 and mip_gap > 0.1:
        actions.append({'name': 'restart'})
    return {'actions': actions}

verified: True
errors: []
warnings: ['Required features not referenced by policy: k, neighbourhood_size']
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 0.0, 'violation': 0.0, 'runtime': 0.0003418922424316406}
feature_coverage: 0.5
proof_obligations: ["path[1]: fallback satisfied under if mip_gap > 0.18 and fractionality > 0.18, if state.get('stagnation_count', 0) >= 3 and mip_gap > 0.1", "path[2]: fallback satisfied under if mip_gap > 0.18 and fractionality > 0.18, else not (state.get('stagnation_count', 0) >= 3 and mip_gap > 0.1)", "path[3]: fallback satisfied under else not (mip_gap > 0.18 and fractionality > 0.18), if lp_gap < 0.08 and fractionality < 0.12, if state.get('stagnation_count', 0) >= 3 and mip_gap > 0.1", "path[4]: fallback satisfied under else not (mip_gap > 0.18 and fractionality > 0.18), if lp_gap < 0.08 and fractionality < 0.12, else not (state.get('stagnation_count', 0) >= 3 and mip_gap > 0.1)", "path[5]: fallback satisfied under else not (mip_gap > 0.18 and fractionality > 0.18), else not (lp_gap < 0.08 and fractionality < 0.12), if state.get('stagnation_count', 0) >= 3 and mip_gap > 0.1", "path[6]: fallback satisfied under else not (mip_gap > 0.18 and fractionality > 0.18), else not (lp_gap < 0.08 and fractionality < 0.12), else not (state.get('stagnation_count', 0) >= 3 and mip_gap > 0.1)"]
