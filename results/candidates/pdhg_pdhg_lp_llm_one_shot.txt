policy_id: llm_one_shot
origin: baseline
split: train
score: -2.620292244420485
aggregate: {'median_gap': 0.7909533848117086, 'median_violation': 1.7728795371746686, 'median_runtime': 0.0013461112976074219, 'median_primal_residual': 1.7728795371746686, 'median_dual_residual': 0.027324200154548207, 'median_iterations': 60.0, 'fail_rate': 0.0}
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    if state.get('violation', 0.0) > 0.03:
        actions.append({'name': 'scale_tau', 'value': 1.06})
        actions.append({'name': 'scale_sigma', 'value': 0.9})
    elif state.get('gap', 1.0) < 0.08:
        actions.append({'name': 'scale_tau', 'value': 0.96})
        actions.append({'name': 'scale_sigma', 'value': 1.04})
    if state.get('stagnation_count', 0) >= 3:
        actions.append({'name': 'restart'})
    actions.append({'name': 'damp', 'value': 0.9})
    return {'actions': actions}

verified: True
errors: []
warnings: ['Action scale_tau is emitted without reading state feature tau.', 'Action scale_sigma is emitted without reading state feature sigma.', 'Action scale_tau is emitted without reading state feature tau.', 'Action scale_sigma is emitted without reading state feature sigma.', 'Required features not referenced by policy: k, r_d_norm, r_p_norm, sigma, tau']
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 0.6624655174456775, 'violation': 0.7735327613083276, 'runtime': 0.0002009868621826172}
feature_coverage: 0.0
proof_obligations: ["path[1]: fallback satisfied under if state.get('violation', 0.0) > 0.03, if state.get('stagnation_count', 0) >= 3", "path[2]: fallback satisfied under if state.get('violation', 0.0) > 0.03, else not (state.get('stagnation_count', 0) >= 3)", "path[3]: fallback satisfied under else not (state.get('violation', 0.0) > 0.03), if state.get('gap', 1.0) < 0.08, if state.get('stagnation_count', 0) >= 3", "path[4]: fallback satisfied under else not (state.get('violation', 0.0) > 0.03), if state.get('gap', 1.0) < 0.08, else not (state.get('stagnation_count', 0) >= 3)", "path[5]: fallback satisfied under else not (state.get('violation', 0.0) > 0.03), else not (state.get('gap', 1.0) < 0.08), if state.get('stagnation_count', 0) >= 3", "path[6]: fallback satisfied under else not (state.get('violation', 0.0) > 0.03), else not (state.get('gap', 1.0) < 0.08), else not (state.get('stagnation_count', 0) >= 3)"]
