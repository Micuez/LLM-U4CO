policy_id: search_variant_2
problem_family: alm_eq_qp
verified: True
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 0.5066174530418606, 'violation': 0.17732400607901036, 'runtime': 0.00014901161193847656}
errors: []
warnings: ['Required features not referenced by policy: k']
feature_coverage: 0.6666666666666666
proof_obligations: ["path[1]: fallback satisfied under if violation > 0.12, if state.get('stagnation_count', 0) > 4 and gap > 0.02", "path[2]: fallback satisfied under if violation > 0.12, else not (state.get('stagnation_count', 0) > 4 and gap > 0.02)", "path[3]: fallback satisfied under else not (violation > 0.12), if gap < 0.01 and violation < 0.015, if state.get('stagnation_count', 0) > 4 and gap > 0.02", "path[4]: fallback satisfied under else not (violation > 0.12), if gap < 0.01 and violation < 0.015, else not (state.get('stagnation_count', 0) > 4 and gap > 0.02)", "path[5]: fallback satisfied under else not (violation > 0.12), else not (gap < 0.01 and violation < 0.015), if state.get('stagnation_count', 0) > 4 and gap > 0.02", "path[6]: fallback satisfied under else not (violation > 0.12), else not (gap < 0.01 and violation < 0.015), else not (state.get('stagnation_count', 0) > 4 and gap > 0.02)"]
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    rho = state.get('rho', 1.0)
    violation = state.get('violation', 0.0)
    gap = state.get('gap', 1.0)
    if violation > 0.12:
        actions.append({'name': 'set_rho', 'value': 1.6 if rho < 1.6 else rho})
    elif gap < 0.01 and violation < 0.015:
        actions.append({'name': 'scale_rho', 'value': 0.82})
    if state.get('stagnation_count', 0) > 4 and gap > 0.02:
        actions.append({'name': 'restart'})
        actions.append({'name': 'damp', 'value': 0.78})
    else:
        actions.append({'name': 'damp', 'value': 0.96})
    return {'actions': actions}

origin: mutation
parent: 
score: -0.9686668521372586
