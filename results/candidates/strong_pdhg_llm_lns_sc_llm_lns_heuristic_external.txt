policy_id: llm_lns_heuristic_external
comparison_role: external_llm_lns_heuristic
origin: strong_baseline
split: train
score: -3.653410435683676
aggregate: {'median_gap': 0.8250880198578339, 'median_violation': 2.215266180017801, 'median_runtime': 0.0019321441650390625, 'median_primal_residual': 2.215266180017801, 'median_dual_residual': 1.0016941469626062e-06, 'median_iterations': 80.0, 'fail_rate': 0.0}
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    tau = state.get('tau', 1.0)
    sigma = state.get('sigma', 1.0)
    k = state.get('k', 0)
    ratio = (state.get('r_p_norm', 0.0) + 1e-9) / (state.get('r_d_norm', 0.0) + 1e-9)
    gap = state.get('gap', 1.0)
    violation = state.get('violation', 0.0)
    if violation > 0.045 and k > 2:
        actions.append({'name': 'scale_tau', 'value': 0.86 if tau >= sigma else 0.92})
        actions.append({'name': 'scale_sigma', 'value': 1.18 if sigma <= tau else 1.08})
    elif ratio > 1.42:
        actions.append({'name': 'scale_tau', 'value': 1.12 if gap > 0.08 else 1.05})
        actions.append({'name': 'scale_sigma', 'value': 0.9 if gap > 0.08 else 0.96})
    elif ratio < 0.7:
        actions.append({'name': 'scale_tau', 'value': 0.9 if gap > 0.08 else 0.96})
        actions.append({'name': 'scale_sigma', 'value': 1.12 if gap > 0.08 else 1.04})
    if state.get('stagnation_count', 0) >= 2:
        actions.append({'name': 'restart'})
    if gap < 0.04:
        actions.append({'name': 'damp', 'value': 0.9})
    elif violation > 0.02:
        actions.append({'name': 'damp', 'value': 0.82})
    else:
        actions.append({'name': 'damp', 'value': 1.0})
    return {'actions': actions}

verified: True
errors: []
warnings: []
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 1.0, 'violation': 1.7320508075688772, 'runtime': 0.00021195411682128906}
feature_coverage: 1.0
proof_obligations: ["path[1]: fallback satisfied under if violation > 0.045 and k > 2, if state.get('stagnation_count', 0) >= 2, if gap < 0.04", "path[2]: fallback satisfied under if violation > 0.045 and k > 2, if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), if violation > 0.02", "path[3]: fallback satisfied under if violation > 0.045 and k > 2, if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), else not (violation > 0.02)", "path[4]: fallback satisfied under if violation > 0.045 and k > 2, else not (state.get('stagnation_count', 0) >= 2), if gap < 0.04", "path[5]: fallback satisfied under if violation > 0.045 and k > 2, else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), if violation > 0.02", "path[6]: fallback satisfied under if violation > 0.045 and k > 2, else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), else not (violation > 0.02)", "path[7]: fallback satisfied under else not (violation > 0.045 and k > 2), if ratio > 1.42, if state.get('stagnation_count', 0) >= 2, if gap < 0.04", "path[8]: fallback satisfied under else not (violation > 0.045 and k > 2), if ratio > 1.42, if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), if violation > 0.02", "path[9]: fallback satisfied under else not (violation > 0.045 and k > 2), if ratio > 1.42, if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), else not (violation > 0.02)", "path[10]: fallback satisfied under else not (violation > 0.045 and k > 2), if ratio > 1.42, else not (state.get('stagnation_count', 0) >= 2), if gap < 0.04", "path[11]: fallback satisfied under else not (violation > 0.045 and k > 2), if ratio > 1.42, else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), if violation > 0.02", "path[12]: fallback satisfied under else not (violation > 0.045 and k > 2), if ratio > 1.42, else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), else not (violation > 0.02)", "path[13]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), if ratio < 0.7, if state.get('stagnation_count', 0) >= 2, if gap < 0.04", "path[14]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), if ratio < 0.7, if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), if violation > 0.02", "path[15]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), if ratio < 0.7, if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), else not (violation > 0.02)", "path[16]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), if ratio < 0.7, else not (state.get('stagnation_count', 0) >= 2), if gap < 0.04", "path[17]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), if ratio < 0.7, else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), if violation > 0.02", "path[18]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), if ratio < 0.7, else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), else not (violation > 0.02)", "path[19]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), else not (ratio < 0.7), if state.get('stagnation_count', 0) >= 2, if gap < 0.04", "path[20]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), else not (ratio < 0.7), if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), if violation > 0.02", "path[21]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), else not (ratio < 0.7), if state.get('stagnation_count', 0) >= 2, else not (gap < 0.04), else not (violation > 0.02)", "path[22]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), else not (ratio < 0.7), else not (state.get('stagnation_count', 0) >= 2), if gap < 0.04", "path[23]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), else not (ratio < 0.7), else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), if violation > 0.02", "path[24]: fallback satisfied under else not (violation > 0.045 and k > 2), else not (ratio > 1.42), else not (ratio < 0.7), else not (state.get('stagnation_count', 0) >= 2), else not (gap < 0.04), else not (violation > 0.02)"]
