policy_id: learned_controller
origin: baseline
split: train
score: -1.3026832035128542
aggregate: {'median_gap': 0.7291077832789121, 'median_violation': 0.002708309911536799, 'median_runtime': 0.0017105340957641602, 'median_primal_residual': 0.002708309911536799, 'median_dual_residual': 0.0031564119493770093, 'median_iterations': 70.0, 'fail_rate': 0.0}
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    if state.get('residual_norm', 0.0) > 0.02:
        actions.append({'name': 'damp', 'value': 0.9})
    elif state.get('gap', 1.0) < 0.15:
        actions.append({'name': 'damp', 'value': 0.65})
    else:
        actions.append({'name': 'damp', 'value': 0.8})
    if state.get('stagnation_count', 0) >= 5:
        actions.append({'name': 'restart'})
    return {'actions': actions}

verified: True
errors: []
warnings: ['Required features not referenced by policy: k']
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 0.8332607643253621, 'violation': 0.035516116657302435, 'runtime': 0.00014209747314453125}
feature_coverage: 0.5
proof_obligations: ["path[1]: fallback satisfied under if state.get('residual_norm', 0.0) > 0.02, if state.get('stagnation_count', 0) >= 5", "path[2]: fallback satisfied under if state.get('residual_norm', 0.0) > 0.02, else not (state.get('stagnation_count', 0) >= 5)", "path[3]: fallback satisfied under else not (state.get('residual_norm', 0.0) > 0.02), if state.get('gap', 1.0) < 0.15, if state.get('stagnation_count', 0) >= 5", "path[4]: fallback satisfied under else not (state.get('residual_norm', 0.0) > 0.02), if state.get('gap', 1.0) < 0.15, else not (state.get('stagnation_count', 0) >= 5)", "path[5]: fallback satisfied under else not (state.get('residual_norm', 0.0) > 0.02), else not (state.get('gap', 1.0) < 0.15), if state.get('stagnation_count', 0) >= 5", "path[6]: fallback satisfied under else not (state.get('residual_norm', 0.0) > 0.02), else not (state.get('gap', 1.0) < 0.15), else not (state.get('stagnation_count', 0) >= 5)"]
