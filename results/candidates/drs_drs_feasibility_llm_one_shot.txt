policy_id: llm_one_shot
origin: baseline
split: train
score: -1.401494124149462
aggregate: {'median_gap': 0.7280462800254426, 'median_violation': 0.0024376973587892916, 'median_runtime': 0.0010646581649780273, 'median_primal_residual': 0.0024376973587892916, 'median_dual_residual': 0.002846805954278071, 'median_iterations': 70.0, 'fail_rate': 0.0}
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    residual = state.get('residual_norm', 0.0)
    gap = state.get('gap', 1.0)
    if residual > 0.01 and gap > 0.5:
        actions.append({'name': 'damp', 'value': 0.95})
    elif residual < 0.003 and gap < 0.3:
        actions.append({'name': 'damp', 'value': 0.55})
    else:
        actions.append({'name': 'damp', 'value': 0.78})
    if state.get('stagnation_count', 0) >= 3 and residual > 0.004:
        actions.append({'name': 'restart'})
    return {'actions': actions}

verified: True
errors: []
warnings: ['Required features not referenced by policy: k']
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 0.8281628316181092, 'violation': 0.034355538880133506, 'runtime': 0.0001342296600341797}
feature_coverage: 0.5
proof_obligations: ["path[1]: fallback satisfied under if residual > 0.01 and gap > 0.5, if state.get('stagnation_count', 0) >= 3 and residual > 0.004", "path[2]: fallback satisfied under if residual > 0.01 and gap > 0.5, else not (state.get('stagnation_count', 0) >= 3 and residual > 0.004)", "path[3]: fallback satisfied under else not (residual > 0.01 and gap > 0.5), if residual < 0.003 and gap < 0.3, if state.get('stagnation_count', 0) >= 3 and residual > 0.004", "path[4]: fallback satisfied under else not (residual > 0.01 and gap > 0.5), if residual < 0.003 and gap < 0.3, else not (state.get('stagnation_count', 0) >= 3 and residual > 0.004)", "path[5]: fallback satisfied under else not (residual > 0.01 and gap > 0.5), else not (residual < 0.003 and gap < 0.3), if state.get('stagnation_count', 0) >= 3 and residual > 0.004", "path[6]: fallback satisfied under else not (residual > 0.01 and gap > 0.5), else not (residual < 0.003 and gap < 0.3), else not (state.get('stagnation_count', 0) >= 3 and residual > 0.004)"]
