policy_id: adaptive
origin: baseline
split: train
score: -0.8854410773472157
aggregate: {'median_gap': 0.0, 'median_violation': 0.0, 'median_runtime': 0.001807570457458496, 'median_primal_residual': 0.0, 'median_dual_residual': 0.3104104032379415, 'median_iterations': 60.0, 'fail_rate': 0.0}
source: def policy(state):
    actions = []
    actions.append({'name': 'fallback'})
    if state.get('violation', 0.0) > 0.0:
        actions.append({'name': 'damp', 'value': 1.0})
    elif state.get('fractionality', 0.0) > 0.22:
        actions.append({'name': 'damp', 'value': 0.85})
    else:
        actions.append({'name': 'damp', 'value': 0.7})
    if state.get('stagnation_count', 0) >= 4 and state.get('mip_gap', 0.0) > 0.05:
        actions.append({'name': 'restart'})
    return {'actions': actions}

verified: True
errors: []
warnings: ['Required features not referenced by policy: k, neighbourhood_size']
smoke_ok: True
smoke_reason: 
smoke_diagnostics: {'iterations': 8, 'gap': 0.0, 'violation': 0.0, 'runtime': 0.00022292137145996094}
feature_coverage: 0.5
proof_obligations: ["path[1]: fallback satisfied under if state.get('violation', 0.0) > 0.0, if state.get('stagnation_count', 0) >= 4 and state.get('mip_gap', 0.0) > 0.05", "path[2]: fallback satisfied under if state.get('violation', 0.0) > 0.0, else not (state.get('stagnation_count', 0) >= 4 and state.get('mip_gap', 0.0) > 0.05)", "path[3]: fallback satisfied under else not (state.get('violation', 0.0) > 0.0), if state.get('fractionality', 0.0) > 0.22, if state.get('stagnation_count', 0) >= 4 and state.get('mip_gap', 0.0) > 0.05", "path[4]: fallback satisfied under else not (state.get('violation', 0.0) > 0.0), if state.get('fractionality', 0.0) > 0.22, else not (state.get('stagnation_count', 0) >= 4 and state.get('mip_gap', 0.0) > 0.05)", "path[5]: fallback satisfied under else not (state.get('violation', 0.0) > 0.0), else not (state.get('fractionality', 0.0) > 0.22), if state.get('stagnation_count', 0) >= 4 and state.get('mip_gap', 0.0) > 0.05", "path[6]: fallback satisfied under else not (state.get('violation', 0.0) > 0.0), else not (state.get('fractionality', 0.0) > 0.22), else not (state.get('stagnation_count', 0) >= 4 and state.get('mip_gap', 0.0) > 0.05)"]
